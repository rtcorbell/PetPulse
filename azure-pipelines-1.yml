# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- '*'

pool:
  vmImage: 'windows-latest'

steps:
- script: |
    git clone https://github.com/rtcorbell/PetPulse.git
    cd repo
    git remote set-url --add --push origin https://dev.azure.com/WCOB-ISYS5423/PetPulse%20(F23-P2)/_git/PetPulse01
    git push --mirror
  displayName: 'Mirror GitHub repo and push to Azure DevOps'
